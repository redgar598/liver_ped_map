# run pipeline
tar_make()

# visulize pipeline
tar_visnetwork()

# see saved objects (targets) 
tar_objects()

# load a target for another session
tar_load()

#see a plot when a target is a plot
tar_read()